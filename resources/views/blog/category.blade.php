@extends('layouts.blog')
@section('title',$category->name)
@section('description',$category->description ?: 'Articles et comparatifs de la catégorie '.$category->name)
@section('content')<section class="blog-hero category-hero"><span class="eyebrow">Catégorie</span><h1>{{ $category->name }}</h1><p>{{ $category->description ?: 'Retrouvez toutes nos analyses vérifiées dans cette catégorie.' }}</p></section><section class="blog-listing"><div class="blog-grid">@foreach($articles as $article)<article class="blog-card"><div class="blog-card-top"><span>{{ str_replace('_',' ',$article->type) }}</span><time>{{ $article->published_at?->translatedFormat('d M Y') }}</time></div><h3><a href="{{ $article->public_url }}">{{ $article->title }}</a></h3><p>{{ $article->excerpt }}</p><footer><span>{{ $article->project->name }}</span><a href="{{ $article->public_url }}">Lire →</a></footer></article>@endforeach</div><div class="pagination blog-pagination">{{ $articles->links() }}</div></section>@endsection
