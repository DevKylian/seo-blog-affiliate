@extends('layouts.blog')
@section('title','Guides et comparatifs logiciels')
@section('content')
<section class="blog-hero"><span class="eyebrow">Décider avec de vraies données</span><h1>Des logiciels analysés.<br><em>Des choix mieux éclairés.</em></h1><p>Tarifs vérifiés, limites documentées et comparatifs transparents pour choisir sans jargon.</p></section>
<section class="blog-listing"><div class="category-chips">@foreach($categories as $category)<a href="{{ route('blog.show',$category->slug) }}">{{ $category->name }} <span>{{ $category->articles_count }}</span></a>@endforeach</div><div class="blog-section-head"><div><span class="eyebrow dark">Dernières analyses</span><h2>Guides, avis et comparatifs</h2></div></div><div class="blog-grid">@forelse($articles as $article)<article class="blog-card"><div class="blog-card-top"><span>{{ str_replace('_',' ',$article->type) }}</span><time>{{ $article->published_at?->translatedFormat('d M Y') }}</time></div><h3><a href="{{ $article->public_url }}">{{ $article->title }}</a></h3><p>{{ $article->meta_description }}</p><footer><span>{{ $article->project->name }}</span><a href="{{ $article->public_url }}">Lire l’analyse →</a></footer></article>@empty<div class="blog-empty"><span>✦</span><h2>Les premières analyses arrivent</h2><p>Les contenus validés depuis le Studio apparaîtront ici.</p></div>@endforelse</div><div class="pagination blog-pagination">{{ $articles->links() }}</div></section>
@endsection
